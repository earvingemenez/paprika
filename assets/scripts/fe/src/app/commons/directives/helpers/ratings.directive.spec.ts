import { RatingsDirective } from './ratings.directive';

describe('RatingsDirective', () => {
  it('should create an instance', () => {
    const directive = new RatingsDirective();
    expect(directive).toBeTruthy();
  });
});
